sudo: required
cache: bundler
services:
  - docker
env:
  global:
    - TESTOPTS="-v"
    - TINYTDS_UNIT_HOST=localhost
rvm:
  - 2.1.9
  - 2.2.5
  - 2.3.1
  - 2.4.0
before_install:
  - docker info
  - sudo ./test/bin/setup.sh
install:
  - gem install bundler
  - bundle --version
  - bundle install
  - bundle exec rake ports
script:
  - bundle exec rake ports build ports:debug test
